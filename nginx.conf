# Production nginx reverse proxy for دليل النبك
# Replace `your-domain.com` with your actual domain.

upstream nabk_app {
  server app:3000;
  keepalive 32;
}

# Limit upload sizes to prevent abuse
client_max_body_size 25m;

# Gzip
gzip on;
gzip_vary on;
gzip_proxied any;
gzip_comp_level 6;
gzip_types
  text/plain text/css text/javascript application/javascript
  application/json application/xml image/svg+xml;

server {
  listen 80;
  server_name your-domain.com;

  # Uncomment after configuring TLS:
  # return 301 https://$host$request_uri;

  location / {
    proxy_pass http://nabk_app;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
  }

  location /_next/static/ {
    proxy_pass http://nabk_app;
    proxy_cache_valid 200 1y;
    add_header Cache-Control "public, max-age=31536000, immutable";
  }

  location /api/health {
    proxy_pass http://nabk_app;
    access_log off;
  }
}

# HTTPS — uncomment and configure once certs are mounted into /etc/nginx/certs/
# server {
#   listen 443 ssl http2;
#   server_name your-domain.com;
#
#   ssl_certificate     /etc/nginx/certs/fullchain.pem;
#   ssl_certificate_key /etc/nginx/certs/privkey.pem;
#   ssl_protocols TLSv1.2 TLSv1.3;
#   ssl_prefer_server_ciphers on;
#
#   # ... duplicate the location blocks from the :80 server above ...
# }
